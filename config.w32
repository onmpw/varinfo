// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("varinfo", "for varinfo support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("varinfo", "enable varinfo support", "no");

if (PHP_VARINFO != "no") {
	EXTENSION("varinfo", "varinfo.c");
}

